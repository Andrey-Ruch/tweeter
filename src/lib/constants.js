export const MAX_TWEET_LENGTH = 140;

// Hard-coded for Step 1. Step 3 adds a profile page that lets the user change it.
export const DEFAULT_USERNAME = "andrey";

export const STORAGE_KEY = "tweeter.tweets";
